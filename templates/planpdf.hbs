<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TRIP PLAN</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            line-height: 1.6;
            background-color: #f4f4f4;
            color: #333;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 2.5em;
            color: #2c3e50;
            text-align: center;
            margin-bottom: 20px;
        }

        h2,
        h3 {
            color: #2c3e50;
            margin-bottom: 10px;
        }

        p {
            margin: 10px 0;
        }

        .trip-details {
            margin-bottom: 30px;
            padding: 15px;
            background: #f9f9f9;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .day {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background: #f9f9f9;
        }

        .day h3 {
            margin-bottom: 5px;
            color: #34495e;
        }

        .schedule {
            margin-left: 20px;
        }

        .schedule h4 {
            margin-bottom: 5px;
            color: #34495e;
        }

        .schedule ul {
            list-style-type: disc;
            margin: 10px 0;
            padding-left: 20px;
        }

        .schedule ul li {
            margin-bottom: 5px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Trip Planning</h1>

        <div class="trip-details">
            <h2>Trip Name: <span id="trip-name">{{tripName}}</span></h2>
            <p><strong>Travel Dates:</strong> <span id="travel-dates">{{startDate}} to {{endDate}}</span></p>
        </div>

        <div id="days">
            {{#each schedule}}
            <div class="day">
                <h3>Day {{day}}: <span class="day-title">{{location}}</span></h3>
                <p><strong>Date:</strong> <span class="day-date">{{date}}</span></p>
                <div class="schedule">
                    <h4>Schedule:</h4>
                    <ul>
                        {{#each items}}
                        <li>{{serviceName}} - {{serviceAddress}}</li>
                        {{/each}}
                    </ul>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</body>

</html>